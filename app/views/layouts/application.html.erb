<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>April Template</title>
    <link href="https://fonts.googleapis.com/css?family=Heebo:400,500,700|Fira+Sans:600" rel="stylesheet">
	  <script src="https://unpkg.com/animejs@2.2.0/anime.min.js"></script>
    <script src="https://unpkg.com/scrollreveal@4.0.0/dist/scrollreveal.min.js"></script>
    <%= javascript_include_tag 'main.min.js' %>
    <%= stylesheet_link_tag 'style.css' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/css/bootstrap.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.2/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/js/bootstrap.min.js"></script>
    <style>
    .btn-primary{
      background-color: unset;
    border: none;
    color: #373b3d;
    }
    </style>
  </head>

  <body>
  <!-- .navbar -->
  <nav class="navbar navbar-full navbar-dark bg-primary" style="background-color: unset !important; position: absolute">
    <button class="navbar-toggler hidden-lg-up" type="button" data-toggle="collapse" data-target="#mainNavbarCollapse">
      &#9776;
    </button>
   
    <div class="collapse navbar-toggleable-md" id="mainNavbarCollapse" style="width: 100vw;">
      <ul class="nav navbar-nav pull-lg-right" style="    margin-right: 4vw;">
        <% if !current_user %>
          <li class="nav-item">
            <a class="btn btn-primary btn-md" href="/users/sign_up" style="">register</a>
          </li>
          <span style="width: 5px"></span>
          <li class="nav-item">
            <a class="btn btn-primary btn-md" href="/users/sign_in" style="">sign in</a>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'sign out', destroy_user_session_path, :method => :delete, class: "btn btn-primary btn-md" %>
          </li>
        <% end %>
        <span style="width: 5px"></span>
        <% if current_user %>
        <% if !current_user.investor_status %>
        <li class="nav-item">
          <p style="color: black">Welcome, potential borrower!</p>
          <a class="btn btn-primary btn-md" href="/loan_inquiries" style="">Loan Inquiries</a>
        <li>
        <% else %>
        <li>
          <p style="color: black">Welcome, investor!</p>
        </li>
        <% end %>
        <button class="btn btn-sm" style="color: #3f47f6" onclick="toggle(); window.location.reload(false);">🏦</button>
        <% end %>
<!--        <li class="nav-item">-->
<!--          <a class="btn btn-primary btn-md" href="/contact" style="">contact</a>-->
<!--        </li>-->
<!--        <span style="width: 5px"></span>-->
<!--        <li class="nav-item">-->
<!--          <a class="btn btn-primary btn-md" href="/faq" style="">FAQ</a>-->
<!--        </li>-->
<!--        <span style="width: 5px"></span>-->
<!--        <li class="nav-item">-->
<!--          <a class="btn btn-primary btn-md" href="/investors" style="">Investors</a>-->
<!--        </li>-->
      </ul>
    </div>
  </nav>
  <!-- /.navbar -->
    <%= yield %>

    <style>
      // Make default navbar links display block at choosen breakpoint and adjust styles to fit...
      @media (max-width: 62em) {
        .navbar-nav {
          padding-top: .425rem;
          padding-left: 0.75rem;
        }
        .navbar-nav .nav-item {
          float: none;
        }
        .navbar-brand {
          float: right;
        }
        .navbar-brand,
        .navbar-nav .nav-item {
          display: block;
        }
        .navbar-nav .nav-item + .nav-item {
          margin-left: 0;
        }
        .dropdown-menu {
          position: relative;
          float: none;
        }
      }
    </style>
    <script>
        var csrfToken = $("meta[name='csrf-token']").attr("content");
        var url = "/toggle";
        function toggle() {
            $.ajaxSetup({
                beforeSend: function(xhr, settings) {
                    if (!/^(GET|HEAD|OPTIONS|TRACE)$/i.test(settings.type)) {
                        xhr.setRequestHeader("X-CSRF-Token", csrfToken)
                    }
                }
            });
            $.ajax({
                url: url,
                data: {},
                type: 'GET'
            }).done(function(data){
                console.log(`${data}`);
            });
        }
    </script>
  </body>
</html>
